<%= form_for(@barter) do |f| %>
  <% if @barter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@barter.errors.count, "error") %> prohibited this barter from being saved:</h2>

      <ul>
      <% @barter.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :seller %><br>
    <%= f.text_field :seller %>
  </div>
  <div class="field">
    <%= f.label :selling %><br>
    <%= f.text_field :selling %>
  </div>
  <div class="field">
    <%= f.label :buyer %><br>
    <%= f.text_field :buyer %>
  </div>
  <div class="field">
    <%= f.label :offer %><br>
    <%= f.text_field :offer %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
